# Process monitoring functions

# Check if process is running
if (checkapp("notepad.exe")) {
    println("Notepad is running");
}

# Get process ID
pid = getapppid("notepad.exe");
println("Process ID:", pid);

# Wait for process to start (5 second timeout)
waitforapp("notepad.exe", 5000);

# Wait for process to close
waitforappclose("notepad.exe");

# Kill a process
killapp("notepad.exe");

# Start a new process
startapp("C:\\Windows\\System32\\notepad.exe", "", true);